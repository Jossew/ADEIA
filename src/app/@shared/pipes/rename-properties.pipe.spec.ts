import { RenamePropertiesPipe } from './rename-properties.pipe';

describe('RenamePropertiesPipe', () => {
  it('create an instance', () => {
    const pipe = new RenamePropertiesPipe();
    expect(pipe).toBeTruthy();
  });
});
